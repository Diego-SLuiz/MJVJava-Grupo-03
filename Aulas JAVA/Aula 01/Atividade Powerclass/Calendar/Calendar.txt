A classe Calendar está localizada no pacote java.util desde a versao JDK1.1 e fornece métodos para converter entre um instante específico no tempo e um conjunto de campos
de calendário, como YEAR, MONTH, DAY_OF_MONTH, HOUR e assim por diante, e para manipular os campos de calendário, como obter a data da próxima semana.
Abaixo segue alguns métodos que serão aplicados em cenários como:

A classe Thread está disponível desde a versão 1.0 da linguagem, ela nos permite que iniciamos execuções \ processos paralelos em nossa aplicação. 
Os principais métodos que devemos compreender são:

-> getInstance
static Calendar	getInstance() : Gets a calendar using the default time zone and locale.
static Calendar	getInstance(Locale aLocale) : Gets a calendar using the default time zone and specified locale.
static Calendar	getInstance(TimeZone zone) : Gets a calendar using the specified time zone and default locale.
static Calendar	getInstance(TimeZone zone, Locale aLocale) : Gets a calendar with the specified time zone and locale.
